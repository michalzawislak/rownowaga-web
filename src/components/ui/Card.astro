---
interface Props {
  class?: string;
  style?: string;
}

const { class: className = '', style } = Astro.props;
---

<div
  class={`bg-[var(--color-background-card)] rounded-xl shadow-md hover:shadow-xl transition-all duration-300 card-lift ${className}`}
  style={style}
>
  <slot />
</div>
